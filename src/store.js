import { createStore } from 'vuex';

const store = createStore({
  state: {
    // État de l'application
  },
  mutations: {
    // Mutations pour modifier l'état
  },
  actions: {
    // Actions pour gérer les mutations
  },
  getters: {
    // Getters pour accéder à l'état
  }
});

export default store;
